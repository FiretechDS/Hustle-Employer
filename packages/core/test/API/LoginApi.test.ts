import { LoginApi } from "../../src/employer/adapter/out/LoginApi";

describe('Login integration test', () => {
  const loginApi = new LoginApi()
  test('should return success or unauthorized', () => {
    
  })
  
});
